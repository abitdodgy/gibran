defmodule Oscar do
  @moduledoc ~S"""
  This will include usage instructions for `Oscar`.

  Shortcut functions that bridge `Oscar.Tokeniser` and `Oscar.Counter`.

  ## Examples

    iex> Oscar.tokeniser.tokenise("The Prophet") |> Oscar.counter.token_count
  """
end
